name: SAST

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  security-events: write
  contents: read

jobs:
  sast:
    uses: LegionForge/dev-rig/.github/workflows/node-sast.yml@3209b9c8ceb717f71a08a72fd4afc9debcacbd0d
    with:
      source-dirs: "src"
    permissions:
      security-events: write
